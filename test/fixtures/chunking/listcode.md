# Steps

A procedure with code in the steps.

1. Step 1, which has a command attached to it and some
   explanation text so the item is not trivial.

   ```sh
   umbra_sync --dir ~/vault0 --relay relay.example.invalid:9000 --once  # line 0
   umbra_sync --dir ~/vault0 --relay relay.example.invalid:9000 --once  # line 1
   umbra_sync --dir ~/vault0 --relay relay.example.invalid:9000 --once  # line 2
   umbra_sync --dir ~/vault0 --relay relay.example.invalid:9000 --once  # line 3
   umbra_sync --dir ~/vault0 --relay relay.example.invalid:9000 --once  # line 4
   umbra_sync --dir ~/vault0 --relay relay.example.invalid:9000 --once  # line 5
   ```

2. Step 2, which has a command attached to it and some
   explanation text so the item is not trivial.

   ```sh
   umbra_sync --dir ~/vault1 --relay relay.example.invalid:9000 --once  # line 0
   umbra_sync --dir ~/vault1 --relay relay.example.invalid:9000 --once  # line 1
   umbra_sync --dir ~/vault1 --relay relay.example.invalid:9000 --once  # line 2
   umbra_sync --dir ~/vault1 --relay relay.example.invalid:9000 --once  # line 3
   umbra_sync --dir ~/vault1 --relay relay.example.invalid:9000 --once  # line 4
   umbra_sync --dir ~/vault1 --relay relay.example.invalid:9000 --once  # line 5
   ```

3. Step 3, which has a command attached to it and some
   explanation text so the item is not trivial.

   ```sh
   umbra_sync --dir ~/vault2 --relay relay.example.invalid:9000 --once  # line 0
   umbra_sync --dir ~/vault2 --relay relay.example.invalid:9000 --once  # line 1
   umbra_sync --dir ~/vault2 --relay relay.example.invalid:9000 --once  # line 2
   umbra_sync --dir ~/vault2 --relay relay.example.invalid:9000 --once  # line 3
   umbra_sync --dir ~/vault2 --relay relay.example.invalid:9000 --once  # line 4
   umbra_sync --dir ~/vault2 --relay relay.example.invalid:9000 --once  # line 5
   ```

4. Step 4, which has a command attached to it and some
   explanation text so the item is not trivial.

   ```sh
   umbra_sync --dir ~/vault3 --relay relay.example.invalid:9000 --once  # line 0
   umbra_sync --dir ~/vault3 --relay relay.example.invalid:9000 --once  # line 1
   umbra_sync --dir ~/vault3 --relay relay.example.invalid:9000 --once  # line 2
   umbra_sync --dir ~/vault3 --relay relay.example.invalid:9000 --once  # line 3
   umbra_sync --dir ~/vault3 --relay relay.example.invalid:9000 --once  # line 4
   umbra_sync --dir ~/vault3 --relay relay.example.invalid:9000 --once  # line 5
   ```

5. Step 5, which has a command attached to it and some
   explanation text so the item is not trivial.

   ```sh
   umbra_sync --dir ~/vault4 --relay relay.example.invalid:9000 --once  # line 0
   umbra_sync --dir ~/vault4 --relay relay.example.invalid:9000 --once  # line 1
   umbra_sync --dir ~/vault4 --relay relay.example.invalid:9000 --once  # line 2
   umbra_sync --dir ~/vault4 --relay relay.example.invalid:9000 --once  # line 3
   umbra_sync --dir ~/vault4 --relay relay.example.invalid:9000 --once  # line 4
   umbra_sync --dir ~/vault4 --relay relay.example.invalid:9000 --once  # line 5
   ```

6. Step 6, which has a command attached to it and some
   explanation text so the item is not trivial.

   ```sh
   umbra_sync --dir ~/vault5 --relay relay.example.invalid:9000 --once  # line 0
   umbra_sync --dir ~/vault5 --relay relay.example.invalid:9000 --once  # line 1
   umbra_sync --dir ~/vault5 --relay relay.example.invalid:9000 --once  # line 2
   umbra_sync --dir ~/vault5 --relay relay.example.invalid:9000 --once  # line 3
   umbra_sync --dir ~/vault5 --relay relay.example.invalid:9000 --once  # line 4
   umbra_sync --dir ~/vault5 --relay relay.example.invalid:9000 --once  # line 5
   ```

7. Step 7, which has a command attached to it and some
   explanation text so the item is not trivial.

   ```sh
   umbra_sync --dir ~/vault6 --relay relay.example.invalid:9000 --once  # line 0
   umbra_sync --dir ~/vault6 --relay relay.example.invalid:9000 --once  # line 1
   umbra_sync --dir ~/vault6 --relay relay.example.invalid:9000 --once  # line 2
   umbra_sync --dir ~/vault6 --relay relay.example.invalid:9000 --once  # line 3
   umbra_sync --dir ~/vault6 --relay relay.example.invalid:9000 --once  # line 4
   umbra_sync --dir ~/vault6 --relay relay.example.invalid:9000 --once  # line 5
   ```

8. Step 8, which has a command attached to it and some
   explanation text so the item is not trivial.

   ```sh
   umbra_sync --dir ~/vault7 --relay relay.example.invalid:9000 --once  # line 0
   umbra_sync --dir ~/vault7 --relay relay.example.invalid:9000 --once  # line 1
   umbra_sync --dir ~/vault7 --relay relay.example.invalid:9000 --once  # line 2
   umbra_sync --dir ~/vault7 --relay relay.example.invalid:9000 --once  # line 3
   umbra_sync --dir ~/vault7 --relay relay.example.invalid:9000 --once  # line 4
   umbra_sync --dir ~/vault7 --relay relay.example.invalid:9000 --once  # line 5
   ```

